#include "Util.hh"

namespace gteditor {
namespace gui {
namespace color {

const wxColour lightYellow(255, 255, 240);
const wxColour lightBlue(240, 255, 255);
const wxColour grey(200, 200, 200);
const wxColour yellow(222, 214, 0);
const wxColour green(0, 100, 0);
const wxColour red(180, 0, 0);
const wxColour darkGrey(100, 100, 100);

}
}
}
